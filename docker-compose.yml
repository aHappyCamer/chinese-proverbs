version: "3"
services:
  web:
    build: .
    ports:
      - "5000:80"
    restart: always
    env_file:
      - .env
    volumes:
      - .:/chineseproverbs
    depends_on:
      - db

  db:
    image: postgres
    restart: always
    env_file:
      - .env